---
kind: Service
apiVersion: v1

metadata:
    name: api-v1

spec:
    selector:
        app.kubernetes.io/name: game-room-api
        app.kubernetes.io/version: v1
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: game-room
    type: ClusterIP
    ports:
        - name: http
          port: 8000
          targetPort: http
